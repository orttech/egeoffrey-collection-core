description: earthquake
format: position
icon: globe
retain: short_history
service:
  configuration:
    domain: earthquake.usgs.gov
    query: minmag=5
  mode: pull
  name: earthquake
  schedule:
    minutes: 60
    trigger: interval