calculate: avg_min_max
description: Random number
format: float_2
icon: hashtag
retain: history
service:
  configuration:
    command: echo $RANDOM
  mode: pull
  name: command
  schedule:
    minutes: 5
    trigger: interval
